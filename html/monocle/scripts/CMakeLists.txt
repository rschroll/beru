file(GLOB HTML_MONOCLE_SCRIPTS_SRC_FILES
     RELATIVE ${CMAKE_CURRENT_SOURCE_DIR}
     monocore.js
     monoctrl.js
)

foreach( SRC_FILE ${HTML_MONOCLE_SCRIPTS_SRC_FILES} )
     file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/${HTML_MONOCLE_SCRIPTS_SRC_FILES} DESTINATION ${CMAKE_CURRENT_BINARY_DIR})
endforeach( SRC_FILE )

#Add files to Qt Creator resources, so they are visible in SDK file list
QT5_ADD_RESOURCES (ResourceSources ${HTML_MONOCLE_SCRIPTS_SRC_FILES})
